# HTML Notes

My learning notes and practice while studying **HTML**.  
This repository contains basic concepts, explanations, and examples that I covered while learning HTML fundamentals.

---

### Topics Covered

### 1. HTML Attributes
Examples and usage of common HTML attributes, including:

- **href** attribute  
- **src** attribute  
- **style** attribute  
- **lang** attribute  
- **title** attribute  

These attributes are used to provide additional information about HTML elements and control their behavior or appearance.

---

### 2. HTML Headings
HTML headings are titles or subtitles displayed on a webpage.

- Defined using `<h1>` to `<h6>` tags  
- `<h1>` is the most important heading  
- `<h6>` is the least important heading  
- Headings help structure content  
- Search engines use headings to understand page structure  

Includes examples showing heading hierarchy and usage.

---

### 3. HTML Paragraphs
HTML paragraphs are defined using the `<p>` tag.

Key concepts covered:

- Default paragraph behavior  
- How browsers ignore extra spaces and lines  
- Line breaks using the `<br>` element  
- Horizontal rules using the `<hr>` element  
- Preformatted text using the `<pre>` element  

Examples demonstrate how text is rendered differently depending on the element used.

---

### 4. HTML Styles
The `style` attribute is used to apply CSS directly to HTML elements.

Topics include:

- **Background color** (`background-color`)  
- **Text color** (`color`)  
- **Font family** (`font-family`)  
- **Font size** (`font-size`)  
- **Text alignment** (`text-align`)  
- Applying multiple styles in one element  

This section explains inline styling and its syntax.

---

### 5. HTML Text Formatting
HTML provides elements to format text and give it semantic meaning.

Formatting elements covered:

- `<b>` – Bold text  
- `<strong>` – Important text  
- `<i>` – Italic text  
- `<em>` – Emphasized text  
- `<mark>` – Highlighted text  
- `<small>` – Smaller text  
- `<del>` – Deleted text  
- `<ins>` – Inserted text  
- `<sub>` – Subscript text  
- `<sup>` – Superscript text  

Differences between visual formatting and semantic meaning are explained with examples.

---

### 6. HTML Quotation and Citation Elements
Elements used for quotations, references, and text direction.

Covered elements:

- `<q>` – Inline quotations  
- `<blockquote>` – Block quotations  
- `cite` attribute in `<blockquote>`  
- `<cite>` – Title of a work  
- `<abbr>` – Abbreviations and acronyms  
- `<address>` – Contact information  
- `<bdo>` – Bidirectional text override  

Each element includes explanation, syntax, and key points.

---

---

### 7. HTML Colors
HTML colors are used to define the color of text, backgrounds, borders, and other elements.

Colors can be applied using the `style` attribute and CSS color properties.

Color formats covered:

- **Color Names**  
  Predefined color names such as `red`, `blue`, `green`, `black`, etc.

- **RGB Values**  
  Uses red, green, and blue values.  
  Example: `rgb(255, 0, 0)`

- **HEX Values**  
  Hexadecimal representation of colors.  
  Example: `#ff0000`

- **HSL Values**  
  Defines colors using hue, saturation, and lightness.  
  Example: `hsl(0, 100%, 50%)`

Common uses of colors in HTML:

- Text color using `color`  
- Background color using `background-color`  
- Applying colors to different HTML elements  

Examples demonstrate how different color formats affect the appearance of a webpage.

---

### 7. HTML + CSS Basics
CSS (Cascading Style Sheets) is used to control the appearance of a webpage, while HTML defines its structure.

Topics covered:

- Purpose of CSS  
- Meaning of “cascading”  
- How CSS affects HTML elements  

Examples demonstrate how CSS styles HTML content.

---

## CSS Types
Different ways to apply CSS to HTML elements.

Covered methods:

- **Inline CSS** – using the `style` attribute  
- **Internal CSS** – using the `<style>` tag in `<head>`  
- **External CSS** – using a linked `.css` file  

Each method is explained with syntax and use cases.

---

## Basic CSS Properties
Common CSS properties used for styling elements.

Properties covered:

- Text color (`color`)  
- Font family (`font-family`)  
- Font size (`font-size`)  
- Borders  
- Padding  
- Margin  

Examples show how spacing and appearance are controlled using CSS.

## Purpose of This Repository

- Practice HTML fundamentals  
- Maintain structured learning notes  
- Build a strong foundation for CSS and JavaScript  
- Track personal learning progress  

---

## Status

- Beginner-friendly  
- Continuously improving and expanding  

---
## Run the HTML File and read the code and output simultaniously
Feel free to explore the files and learn along with the examples.
